"""Tests for the service layer.

An `__init__.py` is required in every test subpackage: without one, mypy's strict pass
sees two `conftest` modules with the same fully qualified name, reports a duplicate, and
**silently stops checking** rather than failing loudly.
"""
